get tokenLengths from the class, it's already been made.
create a variable for the current number of asterisks in use (80)


add all the lengths together (or just... keep a count during processing)
sort the token lengths from highest values to lowest values.
create a map which will store an int and a string as key-value pairs (this is the histogram)
loop through the sorted tokenLengths
	the first value will ALWAYS have the max number of asterisks, so we don't care about that one...
	if past the first value:
		divide the length by the sum of all token lengths, this is now the "current value"
		divide the current value by the previous value
		multiply the above quotient by the current number of asterisks in use
		store the current number of asterisks in use for future reference
		assign a value to the histogram, where the key is the length and the string is asterisks equal to the current number of asterisks in use
	else:
		Just divide the first value's length by the sum of all token lengths, this is now the "current value"
		^This block should only happen during the first value anyway, so...
	store the current value to be used as the previous value for the next iteration
	





